# M001 Audit Evidence — AUDIT-0001

**PID:** `docs/pids/M001-FOUNDATION-AND-ORGANISATION-PROFILE.md`
**Commit audited:** `8daf41d90e8e08910ba5f5b8163881d4ab5aecbb`
**PR:** see repository PR for `wo/M001-foundation-org-profile` → `main`
**Auditor:** fresh Claude Code `Agent` dispatch (general-purpose), zero inherited
context from the Engineer dispatch that produced the commit.
**Verdict:** **GREEN**

---

## Auditor dispatch prompt (verbatim)

Preserved per `SKILL.md#8-deliver` — the audit's independence is checkable
against this, not asserted.

```
You are dispatched as a FORGE Auditor (independent review subagent) for the "Infosecurs" project. You have NO knowledge of how this was built or any prior conversation about it — treat everything below as your only briefing, and verify everything yourself from the repository and the PID, not from anyone's narrative.

FIRST: read `/srv/forge/.claude/agents/forge-auditor.md` in full and follow it exactly — it is your binding contract, including the mandatory real-browser verification gate, the evidence block format, and the verdict rules.

## Your assigned worktree (the ONLY path you may operate in)

`/srv/openclaw/worktrees/infosecurs/m001-auditor`

This is a detached-HEAD checkout at the exact commit under audit. Never touch `/srv/infosecurs` (the shared PROJECT_ROOT) or any other `/srv/openclaw/worktrees/infosecurs/*` path — those are not yours.

**Distinguishing check (confirm before doing anything else):** run `git log -1 --oneline` in your worktree and confirm it shows commit `8daf41d` ("[M001] Foundation & Organisation Profile"). If it does not, stop and report `blocked` — you have been handed the wrong repository's worktree.

commit_sha to record in your evidence block: `8daf41d90e8e08910ba5f5b8163881d4ab5aecbb`

## What to read before auditing

1. `PID.md` — master PID (presentation target, invariants)
2. `docs/pids/M001-FOUNDATION-AND-ORGANISATION-PROFILE.md` — this is the authoritative spec for what you're auditing. Section 13 ("Browser acceptance") lists the exact interaction sequence FORGE requires — follow it.
3. `docs/architecture/ARCHITECTURE.md`, `docs/delivery/GITHUB-EVIDENCE-CONTRACT.md` for context

## Presentation target and gate applicability

Root PID.md §5 declares `PRODUCT_USABLE`. The diff under audit touches Django templates, CSS and routes producing HTML (the Organisation Profile UI). **The mandatory real-browser verification gate in forge-auditor.md therefore applies — this is not optional.** Source-only review, however clean, cannot issue a GREEN here.

## Methodological instruction (applies to any audit, not specific to this diff)

A criterion like "zero unexplained console errors" requires you to establish base-commit behaviour before attributing any console output to this diff — check what the app logs on a clean, unauthenticated load before you drive any interaction, so you can tell base noise from something the diff introduced.

## How to stand the app up

From your worktree: copy `.env.example` to `.env` and fill in real local values yourself (generate a real Django secret key, pick a local dev Postgres password) — this is synthetic local config, not a secret you need from anyone. Then `docker compose up -d --build`. Bootstrap a Customer Zero user via `docker compose exec web python manage.py create_customer_zero` (reads `CUSTOMER_ZERO_USERNAME`/`CUSTOMER_ZERO_EMAIL`/`CUSTOMER_ZERO_PASSWORD`/`CUSTOMER_ZERO_ORGANISATION_NAME` from your `.env` — set them yourself to something synthetic). Fingerprint what you connect to (e.g. confirm the running container's image was built from this exact commit — check `docker compose images` / image digest or a content check — before trusting anything it serves). Use a disposable Playwright + Chromium install for the actual browser-driven checks per forge-auditor.md's "Staying lightweight" section.

Drive the M001 §13 sequence: sign in as Customer Zero → open/select the synthetic organisation → open Organisation Profile → complete representative fields across all sections → save → verify visible confirmation → reload → verify persistence → change one value → save/reload/verify → submit at least one invalid value and verify visible validation → attempt to reach a second synthetic organisation's data via manipulated ID/URL and verify it's blocked → confirm no unexplained console errors. You will need to create a second synthetic organisation yourself (e.g. via the product's own "create organisation" UI, logged in as the same user, so you have two orgs to test cross-tenant boundaries against — or create a second user/org via Django shell/fixtures if that's cleaner; your call, but state which you did).

Also run the project's own mechanical test suite yourself (`docker compose exec web pytest -v`) as a second, independent signal alongside your browser work — but remember source/server tests passing is never sufficient by itself for a PRODUCT_USABLE GREEN; the browser gate is load-bearing.

## Cleanup

Before you finish: stop and remove whatever `docker compose` stack you started (`docker compose down -v`), verify ports 8000/5432 are actually free by checking the port (not trusting the shutdown signal), and remove any Playwright/browser artifacts you don't need to keep as evidence.

## What to return

The exact structured evidence block forge-auditor.md specifies (AUDIT_VERDICT, commit_sha, server_fingerprint_verified, port_released_verified, non_vacuity_established, real_browser_reproduction_completed, root_cause_identified, one line per acceptance criterion, console_errors, findings, confidence), plus the literal interaction sequence you drove (specific enough to replay), before/after state for each step with a visible transition, and your independent mechanical-test-suite result. Verdict: GREEN, PRODUCT RED, STARTUP_FAILURE, or INCONCLUSIVE per forge-auditor.md's rules.
```

---

## Evidence block (as returned by the Auditor)

```
AUDIT_VERDICT: GREEN
commit_sha: 8daf41d90e8e08910ba5f5b8163881d4ab5aecbb
server_fingerprint_verified: yes
port_released_verified: yes
non_vacuity_established: yes
real_browser_reproduction_completed: yes
root_cause_identified: n/a (no defect found)
sign_in_as_customer_zero_verified: yes
open_assigned_organisation_verified: yes
open_organisation_profile_verified: yes
complete_representative_fields_all_sections_verified: yes
save_verified: yes
visible_confirmation_verified: yes
reload_reopen_verified: yes
values_persist_verified: yes
change_one_value_save_reload_verify_verified: yes
invalid_value_visible_validation_verified: yes
cross_tenant_manipulated_id_blocked_verified: yes
no_unexplained_console_errors_verified: yes
console_errors: 2 entries, both explained -- "Failed to load resource: 404" fired
  exactly when the script deliberately GETs a foreign organisation's URLs (org B)
  as Customer Zero to prove the tenant boundary holds. Baseline unauthenticated
  load (step 0) produced zero console messages.
findings: none
confidence: high
```

### Server fingerprint

sha256 of `organisations/views.py`, `organisations/models.py`, `PID.md`, and
the M001 PID as served inside the running container, compared against the
same files on disk at `8daf41d` — all four matched before any interaction was
trusted.

### Non-vacuity

Defect-class check on an existing feature, not a bare-feature-absent case: the
Auditor stripped `memberships__user=user` from
`get_member_organisation_or_404` in a throwaway copy (never the audited
worktree) and reran `organisations/tests/test_tenant_isolation.py` — 4 of 10
tests failed exactly as expected. This is the third independent confirmation
of this same isolation property in this delivery: the Engineer's own
revert-and-rerun (forge-engineer.md rule 14), the PL's independent full-suite
run against the committed worktree, and this Auditor's own differential.

**Self-caught process note, kept for the record:** during the differential
experiment the Auditor's own worktree briefly showed the broken/unscoped
`views.py` on disk (mechanism not confirmed — `cp -r`/`docker cp` handling of
the throwaway copy). Caught immediately via `git status`/`git diff`, restored
with `git checkout -- organisations/views.py`, re-verified the sha256 against
the pre-experiment fingerprint, and reran the full suite (38/38) before any
browser interaction. The audited worktree (`/srv/openclaw/worktrees/infosecurs/m001-auditor`)
was independently confirmed clean (`git status`/`git diff --stat HEAD`, zero
diff from `8daf41d`) by the PL after the Auditor's report, per SKILL.md §7's
routine spot-check requirement.

### Mechanical test suite (independent signal)

`docker compose exec web pytest -v` against the fingerprint-matched container:
**38/38 passed.**

### Literal interaction sequence

Base URL `http://localhost:8000`. Synthetic Customer Zero `auditor_customerzero`.
Synthetic org A `13771430-c3e1-48d3-b7eb-0344d0ec1a06` (member). Synthetic org B
`17661229-6c9b-4fd8-a416-788dc6405987` (foreign tenant, no membership).

1. GET `/` unauthenticated → redirect to `/accounts/login/`. 0 console messages (base-noise baseline).
2. Fill `#id_username`/`#id_password`, submit → redirect to `/organisations/`.
3. GET `/organisations/13771430.../` → org A detail, heading "Auditor Synthetic Org A".
4. Click "Start profile" → `/organisations/13771430.../profile/`, empty form.
5. Fill all 16 fields across Identity/Working model/Technology/Data/Assurance context.
6. Click "Save profile" → 302 → profile page shows green "Organisation profile saved." banner.
7. Fresh GET of the profile URL → all 5+ fields confirmed persisted as saved.
8. Change staff_count 23→47 and working_model hybrid→remote, save, reload → both confirmed changed.
9. Submit `legal_trading_name=""`, `staff_count="-9"` → re-render (no redirect), error banner, both field errors visible inline; reload confirms prior valid data was not corrupted by the rejected submission.
10. GET `/organisations/17661229.../` (org B, manipulated ID) as Customer Zero → **HTTP 404**, no org B content in response.
11. GET `/organisations/17661229.../profile/` → **HTTP 404**; org B's legal name confirmed absent from the page.
12. GET `/organisations/` (list) → contains org A, does not contain org B.

Full replayable script: `M001-AUDIT-0001-interaction-script.js` (this directory).

### Screenshots (before/after state)

Representative subset retained in `M001-AUDIT-0001-screens/` (full 15-shot set
was captured; these are the ones with a visible state transition worth
keeping durably):

| File | What it shows |
|---|---|
| `01c-after-login.png` | Authenticated, redirected to organisation list |
| `04-profile-form-filled.png` | All 16 profile fields completed pre-save |
| `05-after-save-confirmation.png` | Visible "Organisation profile saved." banner |
| `06-reload-persisted.png` | Fresh reload showing saved values still present |
| `07-after-change-reload.png` | Edited value (staff_count 47, working_model remote) persisted after reload |
| `08-invalid-submission-errors.png` | Visible inline validation errors, nothing saved |
| `09-cross-tenant-detail-blocked.png` | 404 on manipulated cross-tenant URL |
| `11-org-list-no-org-b.png` | Organisation list correctly excludes the foreign tenant |

### Non-release-blocking observation

Cross-tenant 404 responses render Django's `DEBUG=True` technical 404 page
(URLconf pattern list visible) because local `DJANGO_ENV=development` sets
`DEBUG=True` per `config/settings.py` — expected for Beta/dev per
`ARCHITECTURE.md`'s "Identity during Beta" and PID §9.10 (production posture
is a separate later gate). Confirmed directly: no org B data appears in the
debug page. Not release-blocking for M001.

---

## PL adjudication

Accepted as GREEN without modification. Spot-checked directly by the PL
(routine per `SKILL.md#7-adjudicate`, not because anything looked
suspicious): confirmed the Auditor's worktree is clean against `8daf41d`
(zero diff), and confirmed all 15 claimed screenshots plus the interaction
script exist on disk exactly as described before selecting the representative
subset above for the durable record.
