# M002 Audit Evidence — AUDIT-0002 (corrected, post-amendment architecture)

**PID:** `docs/pids/M002-SECURITY-BASELINE-AND-INITIAL-RISK.md` §0 (Central Architecture amendment) + §22 (corrected sequence, see below)
**Commit audited:** `7348ecfa73160e18aa6d0919c6377dccf7ebf5fb`
**Auditor:** fresh Claude Code `Agent` dispatch (general-purpose), zero inherited context from any Engineer dispatch.
**Verdict:** **GREEN**

This supersedes AUDIT-0001 in scope only insofar as the underlying product
architecture changed (§0 amendment) — it is not a re-audit of M001, which
remains PRODUCT_GREEN and untouched by this work.

PID §22's original browser-acceptance sequence described the pre-amendment
open-generation flow and does not match the corrected architecture. The
Auditor was given, and followed, a corrected sequence covering the same
intent (full journey, AI-suggested-vs-confirmed wording, persistence,
regeneration safety, tenant isolation) adapted to what §0 actually
requires — reproduced in full below per SKILL.md §8 ("the Auditor's
dispatch prompt goes into the evidence trail verbatim").

---

## Evidence block

```
AUDIT_VERDICT: GREEN
commit_sha: 7348ecfa73160e18aa6d0919c6377dccf7ebf5fb
server_fingerprint_verified: yes
port_released_verified: yes
non_vacuity_established: yes
real_browser_reproduction_completed: yes
root_cause_identified: n/a (feature audit, no defect found)
confidence: high
```

Full per-criterion breakdown (18 items, all `yes`): app started from the
Auditor's own worktree build; fingerprint verified by content hash, not
port-answering alone; login; profile; baseline all five answer states
persisted; asset suggestions reviewed/confirmed/edited/dismissed; asset
detail page shows the correct filtered question subset; single-canonical-
answer round trip proven in both directions; deterministic "Find candidate
risks" produces AI-suggested-labelled drafts with visible grounding; no
forbidden wording (verified/certified/compliant/audited) anywhere; AI
interpretation (via a disposable stub, see below) visibly changes fields
without creating a new row or auto-confirming; risk detail shows grounding/
assumptions/rationale; edit-then-confirm; dismiss; reload persistence;
regeneration does not overwrite confirmed or resurrect dismissed (DB-
verified); cross-tenant manipulated IDs return 404 for all 7 attempted
shapes; console errors: zero unexplained (the only errors captured were the
browser's own report of the 404s the cross-tenant probe was deliberately
sent to receive).

## Fingerprint

Built fresh from a unique project name/volume (the host had several stale
`infosecurs*` stacks from other recent dispatches — none reused). Confirmed
content-identity, not just "a port answered":
```
sha256(risk_register/scenario_engine.py)        worktree == container
sha256(ai_platform/interpretation_contracts.py) worktree == container
```
Fresh migration log on a brand-new volume confirmed no stale state.

## Disposable stub gateway (AI interpretation step)

The Auditor has no live gateway credential (by design — the live AI-quality
evaluation is a separate, already-completed step,
`docs/evidence/M002-LIVE-EVALUATION-CORRECTED.md`). For the real-browser
"Ask AI to review drafts" click, the Auditor stood up a disposable local
HTTP stub mimicking `ai_platform/gateway.py`'s OpenAI-compatible envelope
exactly (verified against that file and `ai_platform/interpretation_contracts.py`
before writing it — full script: `M002-AUDIT-0002-stub-gateway.py`,
alongside this file). Every call was logged; the actual captured call for
this run sent candidate indices `[1, 2]` and received interpretation
indices `[1, 2]` — exact match, values deliberately altered from the input
(capped `current_impact/likelihood + 1`, `[STUB]`-marked rationale/
treatment) so a before/after diff is unambiguous rather than an accidental
echo.

**Self-caught non-vacuity proof, disclosed by the Auditor:** the stub's
first bind attempt (`127.0.0.1` only) was unreachable from the Docker
container network, and the interpretation values stayed at their pre-
interpretation state on that first attempt — proving the check *could*
fail, not just that it happened to pass. Fixed the bind address
(`0.0.0.0`), re-ran clean. The PL treats this kind of disclosed near-miss
as a positive signal, not a flaw in the audit.

## Single-canonical-answer round trip (PID §0.5, non-negotiable)

Driven in both directions, not just an initial-load check:
1. Asset detail page, `mfa_user_accounts` read as `no` (matching the
   general baseline page's earlier-saved value).
2. Changed to `yes` **from the asset page**, saved.
3. Reloaded the **general baseline page** → shows `yes`.
4. Changed back to `no` **from the general baseline page**, saved.
5. Reloaded **the asset page** → shows `no`.

Screenshots: `015_asset_detail_after_change_to_yes.png`,
`016_general_baseline_after_asset_change.png`,
`017_asset_detail_after_restore.png`.

## Regeneration safety (PID §15/§0.6, DB-verified)

After confirm-one/dismiss-one, a second "Find candidate risks" click
produced the UI message *"did not propose any new risks... every
applicable scenario for your confirmed assets and baseline answers was
already generated previously."* Independently confirmed at the database
level (Django shell against the running audited container, not the UI's
own claim): exactly 2 `Risk` rows total, unique `(scenario_id,
key_asset_id)` dedup keys, the confirmed row's edited/interpreted 4/4
impact-likelihood values intact, the dismissed row still `status=dismissed`.
Screenshot: `028_risk_list_after_regeneration.png`.

## Cross-tenant isolation

Second synthetic tenant built fresh in a new browser context. 7 distinct
manipulated-URL shapes attempted against organisation 1 while authenticated
only as organisation 1's own user, including organisation 1's own org id
combined with organisation 2's risk id — **all 7 returned HTTP 404**, never
403, never real cross-tenant data. Screenshot:
`035_crosstenant_org1_id_with_org2_risk_id.png`.

## One documentation-only note (not a finding, already corrected)

`risk_register/interpretation_views.py`'s module docstring (written during
the M002-3c dispatch, before PL integration wired the button in) described
the "Ask AI to review drafts" button as not yet wired into the UI. It was
wired in during PL integration of Phases 3c/3d, and the Auditor confirmed
it works end-to-end. The stale docstring was corrected as part of landing
this evidence file.

## Auditor dispatch prompt (verbatim)

```
You are dispatched as a FORGE Auditor (independent review subagent) for the "Infosecurs" project. You have NO knowledge of how this was built or any prior conversation about it — treat everything below as your only briefing, and verify everything yourself from the repository and the PID, not from anyone's narrative.

FIRST: read `/srv/forge/.claude/agents/forge-auditor.md` in full and follow it exactly — it is your binding contract, including the mandatory real-browser verification gate, the evidence block format, and the verdict rules.

## Your assigned worktree (the ONLY path you may operate in)

`/srv/openclaw/worktrees/infosecurs/m002-auditor`

Detached-HEAD checkout at the exact commit under audit. Never touch `/srv/infosecurs` or any other `/srv/openclaw/worktrees/infosecurs/*` path.

**Distinguishing check (confirm before doing anything else):** `git log -1 --oneline` must show commit `7348ecf` ("[M002 Phase 3f] Corrected live AI evaluation GREEN - durable evidence"). If it does not, stop and report `blocked`.

commit_sha to record in your evidence block: `7348ecfa73160e18aa6d0919c6377dccf7ebf5fb`

## What to read before auditing

1. `PID.md` — master PID (presentation target, invariants)
2. `docs/pids/M002-SECURITY-BASELINE-AND-INITIAL-RISK.md` **section 0 in full** — this is a Central Architecture amendment that substantially rewrote M002's architecture after §1-§27 were originally authorised. Read §0 first; it supersedes the specific parts it names. **Section 22's original browser-acceptance sequence describes the PRE-amendment flow (open-ended AI risk generation) and is now stale in several steps — do not follow it literally.** Use the corrected sequence below instead, which covers the same intent (full user journey, AI-suggested-vs-confirmed distinction, tenant isolation, persistence, regeneration safety) adapted to the architecture §0 actually requires.
3. `docs/pids/M002-AMENDMENT-2026-09-23-REPLAN.md` — background on what changed and why (non-authoritative planning doc, but useful context).
4. `docs/evidence/M002-LIVE-EVALUATION-CORRECTED.md` — the already-completed, separate live AI-quality evaluation against the real gateway (8/8 green, includes a proven regression check that the model never leaks a UUID-shaped identifier). **This already covers deep AI-output-quality evidence — your job is the real-browser user-journey/UI/tenant-isolation verification, not re-proving AI response quality.**

## Presentation target and gate applicability

Root PID.md §5 declares `PRODUCT_USABLE`. This diff touches templates/routes extensively (asset detail pages, risk register UI, security baseline). **The mandatory real-browser verification gate applies — not optional.**

## Corrected browser-acceptance sequence (supersedes PID §22's original list)

1. Start the exact audited commit in Docker, from your own worktree checkout — never the shared `/srv/infosecurs` path.
2. Sign in as a synthetic Customer Zero user you create.
3. Open a completed organisation profile (create one if needed).
4. Open Security Baseline (the general page, PID §6 — unaffected by the amendment) — answer representative questions including `yes`, `partial`, `no`, `unknown`, `not_applicable`. Save and reload; verify persistence.
5. Open Key Assets — review the deterministic starter suggestions, confirm at least one (choose one whose category has methodology scenarios — e.g. an identity/productivity or endpoint suggestion), edit one, dismiss one.
6. **Open the confirmed asset's detail page** (new in this amendment, PID §0.5 — REQUIRED, not optional). Verify it surfaces the relevant canonical baseline questions for that asset's category (not the full 12-question list — a filtered subset). Answer one from here, then separately reload the *general* Security Baseline page and verify the same answer appears there — **this is the single-canonical-answer, non-negotiable requirement (PID §0.5): confirm it's the same underlying fact, not just a coincidentally-matching value** (e.g. change the answer from the asset page, reload the general page, confirm the new value shows there too — a real round-trip, not just an initial-load check).
7. Go to Risk register. Click "Find candidate risks" (the deterministic scenario-instantiation step, PID §0.2/§0.6 — no AI call). Verify draft risks appear, all labelled **"AI suggested"** (never "verified"/"certified"/"compliant"/"audited" anywhere — PID §19), each showing exposure/threat/vulnerability/consequence and a visible grounding/assumptions section.
8. **AI interpretation step**: click "Ask AI to review drafts." You do not have a live gateway credential. Stand up a tiny local disposable HTTP stub server that mimics the OpenAI-compatible `/v1/chat/completions` endpoint `ai_platform/gateway.py`'s `LiteLLMGateway` calls — read that file's `_extract_structured_content`/`_parse_openai_response`/`interpret()` methods to get the exact response envelope shape right: the stub must return JSON with `choices[0].message.content` being a JSON-encoded **string** matching `ai_platform/interpretation_contracts.py`'s `InterpretationResponse.from_response_dict` expected shape (an `"interpretations"` array, one entry per candidate index the request actually sent — you'll need to inspect the incoming request body to read which indices were sent, since the count/indices vary by how many draft risks exist, and echo back a valid `suggested_impact`/`suggested_likelihood`/`rationale`/`suggested_treatment` for each), plus top-level `model`/`usage` fields. Point `AI_GATEWAY_BASE_URL` at your stub (e.g. `http://127.0.0.1:<stub-port>`) and `AI_GATEWAY_API_KEY_FILE` at any local file containing a throwaway string (the stub doesn't need to actually validate it) before starting the app container, then click the button for real in the browser and verify: the draft risks' impact/likelihood/rationale/treatment visibly update; status stays "AI suggested" (never auto-confirmed — PID §2/§0.7 core invariant); no new Risk row was created (same count before/after — AI never originates a persisted risk in M002 V1, PID §0.7).
9. Inspect one interpreted risk's detail page — verify grounding/assumptions/rationale are visible and readable.
10. Edit a draft risk's impact or likelihood, then confirm it (explicit customer action).
11. Dismiss another draft risk.
12. Reload the risk register — verify confirmed/dismissed state persisted.
13. Click "Find candidate risks" again (regeneration) — verify the confirmed risk from step 10 was not overwritten/duplicated, and the dismissed one from step 11 was not resurrected (PID §15 regeneration safety, now enforced by the scenario engine's dedup rule — `risk_register/scenario_engine.py` if you want to read the mechanism).
14. Attempt cross-tenant access: create a second synthetic organisation (with its own user or as a non-member for the first user), and verify a manipulated URL/ID cannot reach the first organisation's assets, baseline answers, or risks (404, not 403 — see `organisations/views.py`'s `get_member_organisation_or_404` for the established pattern this codebase uses throughout).
15. Confirm no unexplained browser console errors across the whole sequence (establish baseline noise on an unauthenticated page load first, per forge-auditor.md's methodology).

### Methodological instruction (applies to any audit, not specific to this diff)

A criterion like "zero unexplained console errors" requires you to establish base-commit behaviour before attributing any console output to this diff — check what the app logs on a clean, unauthenticated load before you drive any interaction.

## Fingerprinting

Before trusting anything the running app serves, confirm the container was built from the exact commit under audit (per forge-auditor.md step 2) — this matters more than usual here since several other Engineer dispatches ran similar Docker stacks recently on this same host; use a content or digest check, not just "a port answered."

## Cleanup

Stop and remove your Docker stack (`docker compose down -v`) and your stub HTTP server. Verify by port, not by trusting the shutdown signal (`ss -ltnp` / `lsof`). Remove any temp credential file you created for the stub.

## What to return

The exact structured evidence block forge-auditor.md specifies. The literal interaction sequence as an ordered step list, specific enough to replay. Before/after state (screenshots preferred) for every step with a visible transition — especially step 6 (single-canonical-answer proof), step 8 (AI interpretation visibly changing draft risk fields without creating new rows or auto-confirming), and step 13 (regeneration not overwriting confirmed/dismissed risks). Your stub server's exact response shape (so the PL can verify it was a faithful stand-in, not something that accidentally made the test trivially pass). Verdict: GREEN, PRODUCT RED, STARTUP_FAILURE, or INCONCLUSIVE per forge-auditor.md's rules.
```

## PL adjudication

Accepted as GREEN. Spot-checked directly (routine per SKILL.md §7, not
because anything looked suspicious): confirmed the Auditor's worktree was
clean and correctly isolated, read the actual stub gateway script and
confirmed it matches the claimed envelope shape exactly, and confirmed the
flagged docstring staleness was real (grepped the file directly) before
accepting the note and correcting it.
