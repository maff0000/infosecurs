# M006 §18 — Sixth Fresh Independent Beta Re-Audit (M006-AUDIT-0006)

**Base:** `main @ 225c0aeecf4f6f898eb2b4b4eeb4f207ae718e76` (PR #56 — L1/K1 correction from M006-AUDIT-0005)
**Candidate:** release image `infosecurs-release:225c0aeecf4f6f898eb2b4b4eeb4f207ae718e76`, Image ID `sha256:afc488184d7bcd749befa88b2a9cfeaab4be82c8203568b168af49f77320a281`
**Binding scope:** `docs/pids/M006-CUSTOMER-ZERO-BETA-HARDENING.md` §18 (all 35 items, complete from-scratch rerun).

A sixth fresh FORGE Auditor — no knowledge of any prior audit's verdict, conclusions,
or methodology beyond reading `M006-AUDIT-0001.md` through `0005.md` purely to
understand what shape of non-vacuous evidence this project expects, never as authority
for its own verdict — executed the full PID §18 acceptance scenario from scratch
(no targeted-audit shortcut) against the release image built from the L1/K1
correction, plus the specific L1/K1 re-verification probes, an adversarial GAP/
NOT_APPLICABLE truth-boundary probe, and a mandatory non-vacuous completion of the
item-22 HTTP-500 requirement that the fifth audit round left PARTIAL.

## Candidate identity (independently confirmed by both the Auditor and the PL)

- Host: `dell-debian`. Repo HEAD at audit time: `225c0aeecf4f6f898eb2b4b4eeb4f207ae718e76`.
- `infosecurs-release:225c0aeecf4f6f898eb2b4b4eeb4f207ae718e76`, Image ID
  `sha256:afc488184d7bcd749befa88b2a9cfeaab4be82c8203568b168af49f77320a281`.
- `org.opencontainers.image.revision` label matches the SHA exactly — confirmed by the
  Auditor at both the start and end of its session, and independently reconfirmed by
  the PL after this round's release-rebuild dispatch (which this time correctly left
  the image in place, unlike the M006-AUDIT-0004 round's own rebuild dispatch, which
  deleted its image at teardown).
- No source bind — confirmed by the Auditor via `docker inspect` Mounts, and
  independently reconfirmed by the PL.

## Verdict

**GREEN — no unresolved Critical/High/Medium/Low defect found.**

All 35 PID §18 items were exercised with non-vacuous, real evidence (HTTP statuses,
DOM/JS assertions, DB queries via Django shell, byte-level PDF/response inspection,
real backup/restore checksums, ~82 screenshots, a complete real-browser journey with
real AI calls). This is the first GREEN verdict across six rounds of this audit
sequence. Given the significance of a first GREEN, the PL performed multiple
independent re-verifications beyond the usual single spot-check (see below) rather
than accepting the report at face value.

### Item 22 — completed non-vacuously this round, not carried forward as PARTIAL

The Auditor made a genuine, sustained, multi-vector effort to trigger a real
unhandled HTTP 500 against the production-configured (`DEBUG=False`) release
container: integer overflow on a numeric field, an embedded NUL byte, an
unconstrained string URL parameter (`security_state`'s `<str:control_key>/` route)
probed with nonexistent keys, path-traversal shapes, and a null byte, a wrong-kind
evidence-download access (external-reference item hit via the file-download URL), a
double-approval race on an already-approved policy version, four simultaneous POSTs
to the same approval endpoint, a 50,000-character unbroken token through the PDF
renderer, and several questionnaire edge-case text inputs (whitespace-only, single
character, emoji-only, 20,000-character single word). Every vector was safely
handled (Django's own field validators, explicit application-level exception
handling, or safe 404s) — no unhandled 500 was found. This is reported as a real,
effortful negative result, not a PARTIAL by omission, per Central Architecture's own
explicit instruction. Item 22's other half (empty states, safe 404s/403s/302s/503s
under real adversarial conditions) is independently, non-vacuously GREEN via items
25/26/27/30/31.

**PL independent re-verification of the item-22 vectors**: personally reproduced the
`security_state` unconstrained-URL-parameter vector directly via Django's test
client against the exact candidate image (a genuinely separate reconstruction, not a
rerun of the Auditor's own script) — all four bad-key shapes (`nonexistent_control_
key_xyz`, `../../etc/passwd`, `%00`, URL-encoded traversal) returned clean 404s, no
500, matching the Auditor's claim exactly.

### L1/K1 (M006-AUDIT-0005) — both re-verified CLOSED, with fresh adversarial rigor

- **L1**: 14 real page loads at 375px, every one carrying a genuinely unbroken
  hostile token in the relevant field(s) across every currently-known surface
  (Evidence, Key Asset, Risk Register, Remediation, Security State, Questionnaire
  response detail, Policy version detail including a hostile-token governance name
  via the external-approval path, and two page-header/organisation-name variants) —
  zero overflow on all 14.
- **K1**: two neutrally-phrased but distinct real AI-drafted SUPPORTED cases produced
  byte-for-byte identical fixed safe text across separate live model calls — strong
  black-box evidence the containment is a fixed application-owned override, not
  drafting luck, independently corroborated by reading the `SUPPORTED_APPLICATION_
  SAFE_ANSWER_TEXT` constant in source. A third SUPPORTED case with real evidence
  attached still produced the same fixed sentence (evidence-count-independent by
  design), plus a correctly separate "Supporting evidence is attached" annotation.
  I2's CONFIRM containment re-confirmed fresh with an adversarial injection case.

**PL independent re-verification**: ran the actual product test suite's three K1
SUPPORTED tests (`test_k1_supported_outcome_with_zero_evidence_fabricated_evidence_
claim_still_persists_safe_text` and its two siblings) directly inside a disposable
container built from this exact candidate image (not the source worktree) — all 3
passed genuinely, confirming the fix is truly baked into the audited artifact itself.

### Adversarial GAP/NOT_APPLICABLE truth-boundary probe — negative result (no equivalent defect found)

Two escalating rounds of adversarial/jailbreak-style prompting against both
un-hardened outcomes (GAP and NOT_APPLICABLE, which Central Architecture's own
disposition deliberately left without an application-owned override this round)
failed to produce any overstated or fabricated customer-facing claim — GAP responses
consistently stated the gap honestly, NOT_APPLICABLE responses never claimed
certification. Reported as a genuine, non-vacuous negative result for Central
Architecture's awareness, not proof the class is closed for all future input.

### I1/I2 — re-proven fresh

I1 (policy PDF review-warnings disclosure) was re-proven with byte-level rigor
across a two-version supersession trace, confirming the only difference between two
downloads of the same superseded PDF was its status label and regeneration
timestamps — all substantive content, including the frozen warning set, was
unchanged. I2 (CONFIRM fixed wording) was re-proven fresh across multiple real cases.

### Process notes (self-disclosed by the Auditor, no product impact)

- The Auditor's own first backup/restore setup attempt reused a Customer-Zero
  organisation name that collided with a hard-coded fixture-command default,
  causing a clean, safe `PolicyLifecycleError` failure (not a crash) — corrected
  immediately with a distinct org name, disclosed transparently.
- Two self-inflicted `405 Method Not Allowed` entries in the Auditor's own global
  error log, from its own exploratory GET requests against POST-only endpoints
  during form-structure discovery — not a real user-reachable path, not a defect.

## PL independent verification

Given this is the first GREEN verdict in six rounds, the PL performed multiple
independent re-verifications rather than a single spot-check:

- Independently reconfirmed candidate identity (Image ID, OCI revision) matches
  exactly, both before and after the Auditor's own session.
- Independently rebuilt/reconfirmed the release image is genuinely still present on
  dell-debian (not deleted, unlike the M006-AUDIT-0004 round's own release-rebuild
  deviation).
- Independently reran Trivy against the exact candidate image — 0 CRITICAL/HIGH,
  matching the release-rebuild dispatch's own claim.
- Independently reconstructed and ran item 22's `security_state` unconstrained-URL
  vector directly via Django's test client against a disposable container built from
  the exact candidate image — all four bad-key shapes returned clean 404s.
- Independently ran the actual product test suite's three K1 SUPPORTED tests
  directly inside a disposable container built from the exact candidate image (not
  the source worktree) — all 3 passed.
- No product/runtime source file touched by this landing — pure audit-evidence.

Per PID §12, a GREEN §18 verdict with no unresolved findings satisfies the
acceptance gate. `M006 §18` moves to **GREEN**. `M006 §19` remains subject to
Central Architecture's own explicit further authorisation before any adversarial
Beta challenge work begins — a GREEN §18 is not itself that authorisation.
